<p id="notice"><%= notice %></p>

<h1>
  <%= @story.title %>
</h1>

<p>
  by <%= @story.author %> at <%= @story.created_at %>
  My name is <%= @myname %>
</p>

<div>
  <%= @story.article %>
</div>

<%= link_to 'Edit', edit_story_path(@story) %> |
<%= link_to 'Back', stories_path %>
